{
  "name": "anoma-intents-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "webapp"
  ],
  "scripts": {
    "dev": "pnpm --filter webapp dev",
    "build": "pnpm --filter webapp build",
    "start": "pnpm --filter webapp start"
  }
"engines": {
  "node": "18.x",
  "pnpm": "8.x"
}
}
